# tuple is immutable, tak bisa diedit!

posisi = (1,6)
# posisi[0] = 2   -> error! tuple tdk bisa diedit

print(posisi[0])
print(posisi[1])

# list of tuples

tuples = [(1,2), (2,2), (3,4)]
print(tuples[1])